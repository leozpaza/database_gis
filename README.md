# База знаний ГИС ЖКХ

Веб-приложение для управляющих компаний — база знаний по обработке обращений граждан через ГИС ЖКХ.

## 🚀 Возможности

- **Быстрый поиск** — мгновенный поиск по инструкциям и шаблонам
- **Категории обращений** — структурированная система тем ГИС ЖКХ
- **Шаблоны ответов** — готовые ответы с возможностью копирования
- **Админ-панель** — управление статьями, категориями, импорт из Excel
- **Тёмная тема** — переключение без перезагрузки
- **Адаптивный дизайн** — работает на всех устройствах

## 🛠 Технологии

**Frontend:** React 18, TypeScript, Vite, TailwindCSS, Framer Motion, TanStack Query, Zustand

**Backend:** Node.js, Express, TypeScript, Prisma ORM, PostgreSQL

## 📦 Установка

```bash
# Клонирование
git clone <repo-url>
cd gis-knowledge-base

# Установка зависимостей
npm run install:all

# Настройка базы данных
cd server
cp .env.example .env
# Отредактируйте .env, укажите DATABASE_URL

# Создание таблиц и заполнение данными
npm run db:push
npm run db:seed

# Настройки клиентского API (для продакшена)
cd ../client
echo "VITE_API_BASE_URL=https://your-backend-host/api" > .env.local
```

## 🚀 Запуск

```bash
# Разработка (frontend + backend)
npm run dev

# Или раздельно:
npm run dev:client  # http://localhost:5173
npm run dev:server  # http://localhost:3001
```

## 📝 Демо-доступ

- **Email:** admin@gis-kb.ru
- **Пароль:** admin123

## 🌐 Деплой на Render.com

1. Создайте PostgreSQL базу данных
2. Создайте Web Service, укажите:
   - Build Command: `npm run install:all && npm run build`
   - Start Command: `npm start`
3. Добавьте переменные окружения из `.env.example`

## 📁 Структура проекта

```
├── client/              # React приложение
│   ├── src/
│   │   ├── components/  # UI компоненты
│   │   ├── pages/       # Страницы
│   │   ├── api/         # API клиент
│   │   └── store/       # Zustand store
├── server/              # Express API
│   ├── src/
│   │   ├── routes/      # API маршруты
│   │   ├── middleware/  # Middleware
│   │   └── utils/       # Утилиты
│   └── prisma/          # Схема БД
└── shared/              # Общие типы
```

## 📄 Лицензия

MIT
